{
  "name": "tms_csv",
  "version": "0.0.1",
  "description": "Transform an eMuseum TMS API into a CSV output for further processing",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf output/csv_*",
    "debug": "node debug src/script/index.js --config searchConfig.json --creds credentials.json",
    "script": "node src/script/index.js --config searchConfig.json --creds credentials.json",
    "service": "node src/service/tmstocsv-pin-service.js --config searchConfig.json --creds credentials.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "github.com/BarnesFoundation/CollectionWebsite.git"
  },
  "keywords": [
    "barnes",
    "tms",
    "csv"
  ],
  "author": "Girlfriends Labs LLC",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-env": "^1.1.8",
    "fast-csv": "^2.3.1",
    "lodash": "^4.17.4"
  },
  "dependencies": {
    "dotenv": "^4.0.0",
    "minimist": "^1.2.0",
    "seneca": "^3.3.0",
    "shelljs": "^0.7.6",
    "socket.io": "^1.7.3",
    "winston": "^2.3.1"
  }
}
